function balancedParens(n) {
    let arrayToReturn = [];
    let newSting = '';
    let auxString = '';
    let randomNums = [];
    for(let i = 0; i < n; i++){ 
        newSting += '()';  
    }
    arrayToReturn.push(newSting);
    for(let caracter of newSting){
        
    }
    
    return arrayToReturn ;
}

balancedParens(3);